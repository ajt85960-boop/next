# Session + Redis 认证与国际化实践笔记

本文基于当前项目实现，记录两部分：
- 认证：`Session + Redis`（登录态管理）
- 国际化：`i18next + react-i18next + i18next-resources-to-backend`（中英文切换）

---

## 一、认证实现（Session + Redis）

### 1. 技术选型
- 服务端框架：Next.js App Router（全栈）
- 数据库：PostgreSQL
- ORM：Prisma
- 缓存：Redis
- 密码加密：bcryptjs

### 2. 数据模型（当前简化版）
文件：`prisma/schema.prisma`

`User`（用户）
- `id`
- `username`（唯一，登录账号）
- `email`（可选，唯一）
- `password`（哈希）
- `role`（`ADMIN | MANAGER | ANNOTATOR`）
- `createdAt` / `updatedAt`

`Session`（会话）
- `id`（session id）
- `userId`
- `expiresAt`
- `createdAt` / `updatedAt`

### 3. 登录流程
文件：`src/app/api/auth/login/route.ts`

1. 前端提交 `username + password`
2. 服务端查用户（按 `username`）
3. `bcrypt.compare` 校验密码
4. 调用 `createSession(user.id)` 生成会话
5. 返回 `sess_id` 到 HttpOnly Cookie

### 4. Session 写入策略
文件：`src/lib/auth.ts`

`createSession(userId)` 会做两层写入：
- 写 PostgreSQL `Session` 表（持久化）
- 写 Redis：
  - key: `sess:{sessionId}`
  - value: `{ userId, role }`
  - TTL: `SESSION_TTL_DAYS`（默认 7 天）

### 5. 请求鉴权流程
`getSessionPayload(sessionId)`：
1. 先查 Redis（快路径）
2. Redis 没命中时查 DB `Session`（慢路径）
3. 若有效，回填 Redis
4. 过期会话会清理并返回未登录

### 6. 登出流程
文件：`src/app/api/auth/logout/route.ts`

- 删除 Redis `sess:{id}`
- 删除 DB `Session` 记录
- 清空 Cookie 并跳回登录页

### 7. 角色访问控制（当前实现）
文件：`src/lib/role.ts`

按 `User.role` 控制模块可见与可访问：
- `ADMIN`: `overview/devices/annotation/people`
- `MANAGER`: `overview/devices/annotation`
- `ANNOTATOR`: `annotation`

页面服务端会再次校验，防止仅靠前端菜单隐藏导致越权。

---

## 二、国际化实现（中英文切换）

### 1. 依赖
- `i18next`：翻译引擎
- `react-i18next`：react里面用t('key')
- `i18next-resources-to-backend`：按需加载语言json
```
pnpm add i18next react-i18next i18next-resources-to-backend
```

### 2. 语言资源
文件：
- `src/locales/zh-CN/common.json`
- `src/locales/en-US/common.json`

约定：两份 JSON 使用相同 key，不同语言 value。

### 3. i18n 初始化
文件：`src/i18n/index.ts`

做了这些配置：
1. `supportedLngs`配置支持语言: `zh-CN`, `en-US`
2. `fallbackLng`默认语言: `zh-CN`
3. 使用 `resources-to-backend` 动态加载对应json：
   - `@/locales/${language}/${namespace}.json`
4. 从 `localStorage.lang` 读取用户上次语言

### 4. 全局注入
文件：`src/components/providers.tsx`

通过 `import "@/i18n"` 完成初始化挂载。

### 5. 页面使用方式
在 Client Component 中：
1. `const { t } = useTranslation()`
2. 把硬编码文案改成 `t("xxx.yyy")`

例如：
- 首页：`src/app/page.tsx`
- 登录：`src/app/login/page.tsx`
- 注册：`src/app/register/page.tsx`
- 侧边栏和顶栏：`src/components/app-shell.tsx`

### 6. 语言切换组件
文件：`src/components/language-switcher.tsx`

核心逻辑：
- `i18n.changeLanguage("zh-CN" | "en-US")`
- 同步 `localStorage.setItem("lang", lang)`

这样刷新后仍保持所选语言。

---

## 三、你可以复用的落地顺序（推荐）

1. 先确定认证模型（User/Session）
2. 打通登录、鉴权、登出三条主链路
3. 再加角色映射（先简单按 role 字段）
4. 最后做国际化（先核心页面，后全量覆盖）

---

## 四、常见问题

### Q1：删除 Redis session 会立刻下线吗？
不一定。当前实现是 Redis 优先、DB 兜底。  
如果 DB 的 `Session` 还有效，请求会回源并重新写回 Redis。

### Q2：为什么不用 URL 语言路由（如 `/zh`）？
当前项目采用客户端切换方案，改造成本更低，适合后台系统。  
若有 SEO 或分享链接需求，可再迁移到 `app/[lang]` 方案。

### Q3：新增页面如何支持国际化？
给 `common.json` 增 key，然后页面里用 `t("key")` 替换文本。

